const express = require('express')
const router = express.Router()

// Placeholder routes - implement CRUD operations as needed
router.get('/', (req, res) => {
  res.json({ message: 'Users routes' })
})

module.exports = router
